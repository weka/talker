version: '3'
services:
  redis:
    image: "redis:4.0.14-alpine"
  talker:
    build:
      context: ../../agent/.
      args:
        - TALKER_AGENT_VERSION=$TALKER_AGENT_VERSION
        - TALKER_HOST_ID=$TALKER_HOST_ID
    volumes:
      - /var/log:/var/log
    links:
      - redis
