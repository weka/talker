version: '3'
services:
  redis:
    image: "redis:4.0.14-alpine"
  talker:
    build:
      context: ../../agent/.
      args:
        - TALKER_VERSION=$TALKER_VERSION
        - TALKER_REDIS_VERSION=3.3.7
        - TALKER_HOST_ID=$TALKER_HOST_ID
    links:
      - redis
