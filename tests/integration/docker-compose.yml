version: '3'
services:
  redis:
    image: "redis:alpine"
  talker:
    build:
      context: ../../agent/.
      args:
        - TALKER_VERSION=1.8.0-eos
        - TALKER_REDIS_VERSION=2.10.6
        - TALKER_HOST_ID=$TALKER_HOST_ID
    links:
      - redis
